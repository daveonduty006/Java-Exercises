import javax.swing.*;
import java.awt.*;

public class Main {

	public static void main(String[] args) {
		/*
		// JFrame= a GUI window to add components to
		JFrame frame= new JFrame(); // creates a frame
		frame.setTitle("JFrame title goes here"); // sets frame's title
		frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE); // exit out of program if frame is 
		                                                      // corner-closed
		frame.setResizable(false); //prevents the frame from being resized by the user
		frame.setSize(420,420); // sets the frame's x and y dimensions
		frame.setVisible(true); // makes frame visible
		
		ImageIcon image= new ImageIcon("logo.png"); // creates an ImageIcon
		frame.setIconImage(image.getImage()); // changes icon of frame
		
		//frame.getContentPane().setBackground(Color.GREEN); // changes color of background to 
		                                                     // default green
		frame.getContentPane().setBackground(new Color(123,50,250)); // creates custom color via RGB
	    */
		
		MyFrame myFrame= new MyFrame();

	}

}
